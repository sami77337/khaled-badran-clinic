// Service worker registration is intentionally disabled until the booking and
// privacy caching strategy is reviewed. This public batch must not cache
// patient data, private medical files, or future authenticated pages.
